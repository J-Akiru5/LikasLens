# **🛠️ LikasLens: VS Code Copilot Skills Library**

**How to use this file:**

Copy the "Instructions" text into your VS Code Copilot Customizations (or .cursorrules / .github/copilot-instructions/). Developers can then trigger these powerful automations simply by typing the Skill command in the chat.

## **👨‍💻 ReD: Backend & AI Lead**

**Focus:** Laravel Architecture, FastAPI, Cosmos DB (Gremlin), Infrastructure, and Testing.

| Skill | Description | Instructions |
| :---- | :---- | :---- |
| /scaffold-gremlin-traversal | Generates complex Cosmos DB graph queries. | "Generate an Azure Cosmos DB Gremlin query to find relationships between laws, violations, and jurisdictions. Ensure the syntax is compatible with gremlin-python and uses async execution." |
| /scaffold-gremlin-upsert | Generates safe Insert/Update graph queries. | "Generate a safe Gremlin 'Upsert' query using the fold().coalesce(unfold(), addV()) or addE() pattern to prevent duplicate data if the migration runs twice." |
| /scaffold-soft-delete | Archives graph data instead of destroying it. | "Generate a Gremlin query that adds an isActive: false and archivedAt timestamp to the target Vertex/Edge instead of using .drop(). Explain how to filter this out in standard traversals." |
| /scaffold-edge-migration | Safely re-routes relationships (e.g., Law shifts). | "Generate a Python script using gremlin-python that finds an old edge, creates a new edge to a new target vertex carrying over all properties, and applies a soft-delete to the old edge." |
| /generate-migration-script | Scaffolds the Python runner for graph updates. | "Generate a standard Python script template for a Graph Database Migration, including logging, an up() function, a down() rollback function, and standard try/catch blocks." |
| /scaffold-pydantic-contract | Translates Laravel JSON payloads to Python. | "Analyze the provided Laravel JSON payload and generate a strict FastAPI Pydantic model, ensuring exact type safety for cross-boundary communication." |
| /scaffold-gemini-triage-agent | Scaffolds Python function for Gemini API calls. | "Generate a Python async function for the Gemini API. Pre-configure it with strict JSON-mode response formatting and exponential backoff/retry logic for handling rate limits." |
| /debug-ai-timeout | Analyzes Python async/await bottlenecks. | "Analyze the provided FastAPI code. Identify bottlenecks where YOLOv8 or Gemini API calls might cause the server to hang, and provide optimized async/await structuring." |
| /generate-azure-dockerfile | Generates optimized Azure Container Dockerfiles. | "Generate a production-ready, highly optimized Dockerfile tailored for Azure Container Apps. Ask me if this is for the PHP-FPM/Laravel service or the Python/FastAPI service first." |
| /scaffold-azure-blob-upload | Scaffolds secure image upload logic in Laravel. | "Generate the Laravel Controller and Service logic needed to stream and securely upload evidentiary images directly to Azure Blob Storage/Supabase without exhausting server RAM." |
| /generate-laravel-endpoint | Scaffolds complete Laravel API vertical. | "Generate the code for a new Laravel 12 API route: 1\) The API Route definition, 2\) The Controller method, and 3\) A strict FormRequest class for validation." |
| /scaffold-supabase-rls | Generates SQL for Supabase Row Level Security. | "Generate the exact PostgreSQL SQL syntax for Supabase Row Level Security (RLS) policies based on the requested table, ensuring citizens can only read their own rows while admins read all." |
| /debug-cross-service-auth | Troubleshoots Next.js to Laravel token handoff. | "Analyze why a JWT generated by Supabase on the frontend is failing validation when hitting the Laravel backend. Suggest headers, CORS, or JWT secret mismatches to check." |
| /generate-phpunit-test | Auto-generates backend test coverage. | "Read the provided Laravel endpoint and generate a PHPUnit/Pest feature test. Include a 'happy path' assertion and two validation failure assertions. Use Mockery if external APIs are called." |

## **🎨 FE1: UI & Styling Specialist**

**Focus:** Next.js UI, Tailwind CSS, Responsive Design, "Ghost Mode" Theme Switching.

| Skill | Description | Instructions |
| :---- | :---- | :---- |
| /scaffold-ui-card | Scaffolds Tailwind component with dark mode. | "Generate a fully responsive, accessible React functional component using Tailwind CSS utility classes. Ensure it supports default styling and dark mode styling via the dark: modifier." |
| /scaffold-theme-provider | Scaffolds global context for theme toggling. | "Generate a React Context provider for Next.js App Router to globally toggle the app between 'Civic Mode' and 'Ghost Mode'. Ensure state persists across page refreshes safely." |
| /debug-client-server-boundary | Fixes Server vs. Client component errors. | "Analyze the Next.js App Router error. Explain exactly where to place the 'use client' directive or how to restructure the component tree to separate server data from client interactivity." |
| /debug-hydration-mismatch | Fixes React HTML hydration errors. | "Diagnose the Next.js hydration mismatch error. Provide the fix, specifically looking for dynamic theming issues, localStorage checks before mount, or invalid HTML nesting." |
| /debug-flex-grid | Fixes broken layouts and explains CSS math. | "Analyze the broken UI layout code. Explain why the Flexbox or CSS Grid classes are failing in simple terms, and provide the corrected Tailwind classes to fix alignment and wrapping." |
| /explain-z-index-stack | Troubleshoots stacking context issues. | "Explain why the requested Tailwind component is hiding behind another element. Provide the exact Tailwind z-index and position classes needed to fix the stacking context." |
| /fix-mobile-overflow | Fixes horizontal scrolling on mobile screens. | "Identify the nested element causing a horizontal scrollbar on mobile viewports. Provide the necessary overflow-hidden, max-w-full, or width adjustments to fix it." |
| /scaffold-interactive-modal | Generates accessible, animated popup modals. | "Generate a fully accessible, animated popup modal component in Next.js using Tailwind. Include trap-focus logic, escape-key closing, and dark-mode support." |
| /generate-skeleton-loader | Scaffolds pulsing UI skeletons for loading. | "Generate a pulsing UI skeleton using Tailwind's animate-pulse that mimics the shape of the requested component (e.g., Scoreboard or Feed) for use during data fetching." |
| /audit-ghost-mode-contrast | Checks accessibility contrast ratios. | "Audit the provided Tailwind component for dark-theme colors. Ensure they pass WCAG accessibility contrast ratios so text remains readable in Ghost Mode, and suggest safer color codes if they fail." |

## **📱 FE2: Hardware & PWA Specialist**

**Focus:** Browser APIs (Camera, GPS), EXIF metadata stripping, next-pwa, Offline resilience.

| Skill | Description | Instructions |
| :---- | :---- | :---- |
| /scaffold-media-devices | Hooks for safe camera/media access. | "Scaffold a robust React hook (useCamera) to access navigator.mediaDevices.getUserMedia(). Include logic for switching cameras and handle iOS Safari permission policies and NotAllowedErrors." |
| /audit-exif-stripper | Security check for Ghost Mode payloads. | "Analyze the image-processing JavaScript. Verify it successfully strips all EXIF metadata (device model, timestamps) while preserving GPS coordinates and image quality. Warn of any privacy leaks." |
| /scaffold-canvas-capture | Freezes video stream to base64/Blob. | "Generate JS logic to freeze a \<video\> stream, draw it to a hidden \<canvas\>, and compress it into a lightweight Blob or base64 string ready for network upload." |
| /debug-hardware-permissions | Troubleshoots why Camera/GPS is blocked. | "Analyze the hardware API failure. Explain why navigator.mediaDevices is undefined (e.g., localhost vs HTTPS) or why permissions are failing, and provide code to prompt the user correctly." |
| /scaffold-next-pwa-config | Generates Next.js PWA configuration. | "Generate the exact next.config.js wrapper and next-pwa workbox configuration needed to cache the Next.js App Router for offline use without breaking dynamic server-side API calls." |
| /generate-pwa-manifest | Scaffolds manifest.json for installation. | "Generate a standard manifest.json file for a PWA. Ensure required fields like theme\_color, background\_color, display: standalone, and the icons array are properly structured." |
| /debug-service-worker | Troubleshoots offline loading/caching errors. | "Analyze the service worker or caching console error. Identify if the Service Worker is aggressively caching the wrong API routes, and provide the workbox regex fix." |
| /scaffold-offline-queue | IndexedDB utility for offline drafts. | "Generate a utility using IndexedDB to temporarily save a citizen's drafted report (Image \+ GPS) if submitted while offline, and queue it to upload automatically when the connection returns." |
| /scaffold-gps-formatter | Formats Geolocation outputs for the API. | "Generate a clean utility function to convert raw GeolocationPosition objects into the exact \[Lat, Long\] string format expected by the backend database." |
| /debug-async-flow | Fixes Promise and async/await errors. | "Analyze the asynchronous JavaScript code. Explain where the Promise chain or async/await flow is failing (race conditions, unhandled rejections). Provide the corrected code block." |

## **🔌 FE3: State & API Connector**

**Focus:** Supabase Auth, React State, API Data Fetching, Error Handling, CORS.

| Skill | Description | Instructions |
| :---- | :---- | :---- |
| /scaffold-supabase-auth-flow | Scaffolds complete Next.js login boilerplate. | "Scaffold Next.js boilerplate for a Supabase login/register component using the @supabase/ssr package for App Router compatibility, ensuring secure HTTP-only cookie management." |
| /scaffold-protected-route | Generates Middleware to protect secure pages. | "Generate Next.js Middleware logic to intercept a page request, verify the Supabase session token, and redirect unauthenticated users back to the login screen safely." |
| /debug-cors-nightmare | Explains CORS errors and provides header fixes. | "Analyze the provided CORS network error. Explain in plain English why the browser blocked the request. Tell the user exactly what headers to check in fetch or what to fix in the Laravel backend." |
| /scaffold-axios-interceptor | Generates API client that attaches tokens. | "Generate a robust API client (using axios or fetch) that automatically intercepts requests, attaches the user's Supabase Bearer token, and handles 401 Unauthorized redirects." |
| /generate-zod-schema | Generates strict Zod schemas from JSON. | "Read the provided Laravel JSON payload expectation and generate a strict Zod validation schema for the frontend form, ensuring we never send a malformed payload across the network." |
| /debug-payload-mismatch | Analyzes 422 Unprocessable Entity errors. | "Analyze the 422 Unprocessable Entity error log. Explain exactly which field in the frontend JSON request does not match the backend rules, and provide the code to fix the frontend state." |
| /debug-useeffect-loop | Fixes infinite re-render fetch loops. | "Diagnose the infinite re-render loop in the provided React component. Explain how the useEffect dependency array is misconfigured during the data fetch, and provide the fixed code." |
| /scaffold-complex-form | Scaffolds React state for multi-step forms. | "Scaffold the React state boilerplate for a multi-step form that combines text input, camera output (File/Blob), and GPS coordinates into a single cohesive payload object ready for upload." |
| /scaffold-optimistic-update | Instantly updates UI before API confirmation. | "Generate React state logic to instantly update the UI (optimistic update) before awaiting the backend API response. Include the rollback logic in the catch block if the API fails." |
| /scaffold-error-boundary | Generates Error Boundary for graceful crashes. | "Generate a React Error Boundary component to catch API crashes or unexpected null data gracefully, displaying a friendly fallback UI instead of crashing the entire React tree." |

