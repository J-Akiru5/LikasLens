"use client";

/**
 * Auth is now pure Supabase — no Laravel sync needed.
 * This file is kept as a no-op for import compatibility.
 */
export function initAuthRefresh() {
  // No-op — Laravel backend removed
}
