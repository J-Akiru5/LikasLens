"use client";

/**
 * Placeholder for auth initialization.
 * Laravel sync and token refresh have been removed — auth is now pure Supabase.
 *
 * Call this once at app startup from a root client component if needed.
 */
export function initAuthRefresh() {
  // No-op: Supabase handles token refresh internally.
}
